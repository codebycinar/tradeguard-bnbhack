# All optional — the backtest and the on-chain gate run with no keys.

# CoinMarketCap (free "Basic" plan): enables Fear & Greed in the strategy signal
# and listing / age / market-cap / volume in the risk gate.
CMC_API_KEY=

# Override the default public BSC RPC if you have a private endpoint.
BSC_RPC_URL=https://bsc-dataseed.bnbchain.org
# BSC_CHAIN_ID=56
